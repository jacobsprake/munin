version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - ./engine/out:/app/engine/out
      - ./data:/app/data
  # Optional: run engine pipeline (manual or cron)
  # engine:
  #   image: python:3.11-slim
  #   working_dir: /app
  #   volumes:
  #     - .:/app
  #   command: python engine/run.py --quick
